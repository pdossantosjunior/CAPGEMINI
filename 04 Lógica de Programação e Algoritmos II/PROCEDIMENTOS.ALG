Algoritmo "Procedimentos"

Var

resultado : inteiro

procedimento somar(i,j : inteiro; letrasemuso : caractere) //coloquei assim só para lembrar como usar variáveis de diferentes tipos
inicio
      resultado <- i + j
fimprocedimento

Inicio

somar(10,20,"a")
escreval(resultado)

Fimalgoritmo